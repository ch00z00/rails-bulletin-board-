<h1>Hello Ruby</h1>

<div class="button-container">
  <%= link_to "Create post →", "posts/new" %>

  <ul>
    <% @posts.each do |post| %>
      <li>
        <h2><%= post.title %></h2>
        <p><%= post.content %></p>
        <span>
          <small><%= post.created_at %></small>
        </span>
      </li>
    <% end %>
  </ul>
</div>

